# 掌术 AI 科研工作站：Agent 任务设计

> 文档版本：v1.0  
> 配套文档：[《掌术 AI 科研工作站：功能规划 PRD》](掌术AI科研工作站_功能规划PRD.md)  
> 架构依据：[《掌术 AI 科研工作站：系统架构设计》](掌术AI科研工作站_系统架构设计.md)  
> 覆盖范围：F-01～F-12 智能助手（Agent）方向

---

## 0. 通用设计约束

### 0.1 Agent 职责边界

| Agent 可以做 | Agent 不可以做 |
|---|---|
| 理解自然语言意图、补齐信息、生成候选计划 | 编造文献、数据、统计数值或期刊信息 |
| 调用白名单工具、组织任务、解释工具结果 | 绕过领域服务直接读写数据库或对象存储 |
| 推荐研究设计、清洗策略和统计方法 | 将推荐意见伪装成已确认的专业结论 |
| 基于 Verified Result 撰写和润色 | 自行计算或修改 HR、OR、P 值、CI、AUC 等结果 |
| 在关键节点请求用户确认 | 未经确认执行高风险清洗、分析、导出或外发动作 |

### 0.2 通用风险等级

| 等级 | 动作示例 | 处理方式 |
|---|---|---|
| R0 只读 | 检索、查看、解释 | 可执行，记录审计日志 |
| R1 可撤销写入 | 保存草稿、创建候选计划 | 执行后通知，可版本回退 |
| R2 数据或计算变更 | 应用清洗、启动分析、调整模型 | 执行前强制确认 |
| R3 正式外发 | 导出正式稿、形成投稿包 | 强确认、权限检查、合规闸门 |

### 0.3 通用任务状态

`信息收集 → 计划生成 → 等待确认 → 工具执行 → 结果校验 → 交付 → 衔接下一任务`

任务产生的正式科研产物必须登记稳定 ID、版本、来源和上下游关系；聊天内容不能作为唯一事实来源。

### 0.4 论文真实流程调用顺序

> F 编号用于能力治理和文档索引，不代表论文流程中的实际发生顺序。Agent 汇报页、入口路由页和总编排器应优先按照下列流程组织。

`F-01 选题规划 → F-02 文献综述 → F-03 研究设计 → F-11 伦理注册 → F-12 研究执行 → F-04 数据清洗 → F-05 算法推荐 → F-06 统计分析 → F-07 统计绘图 → F-08 报告生成 → F-09 文献格式化 → F-10 期刊推荐`

---

## 任务 1：选题规划 Agent（F-01）

### 任务基础信息

- **任务名称**：选题规划任务
- **Agent 名称**：A-选题规划
- **一句话描述**：用户想要找到临床科研创新课题，Agent 帮其挖掘领域热点、生成标准化 PICOS、输出多套差异化选题方案并评估创新可行性。

### 1. 工具清单

| 工具名称（英文） | 工具能力说明 | 需要哪些输入 | 返回什么 |
|---|---|---|---|
| `get_field_hotspot` | 检索领域近 3 年顶刊、科研资助热点与临床争议空白 | 疾病/领域、目标期刊层级、时间范围 | 热点榜单、证据来源、未解决空白 |
| `build_picos_frame` | 将口语化需求转为标准 PICOS 并检查逻辑冲突 | 研究偏好、疾病领域、初步热点 | PICOS 草案、缺失项、冲突提示 |
| `query_dataset_inventory` | 查询用户有权限使用的数据集概况 | 用户 ID、项目 ID、领域标签 | 数据集摘要、字段范围、可用状态 |
| `calculate_innovation_score` | 从新颖性、临床价值、证据缺口、可行性四维量化评估 | PICOS、热点证据、可用数据摘要 | 0～100 分、分项理由、风险 |
| `generate_topic_plan` | 生成创新型、稳妥型、热点型三套方案 | 热点、PICOS、评分、数据条件 | 3 套选题、差异说明、初步大纲 |

### 2. 执行流程

```text
步骤 1：从用户输入获取研究领域、疾病方向、研究偏好、目标期刊、是否有既往数据集
  ↓
步骤 2：并行调用 get_field_hotspot、build_picos_frame、query_dataset_inventory
  ↓
步骤 3：强制中断——展示热点证据、PICOS 草案与可用数据摘要，等待用户确认或修改
  ↓
步骤 4：调用 calculate_innovation_score 完成候选方向评分
  ↓
步骤 5：调用 generate_topic_plan 生成三套差异化选题与初步研究大纲
  ↓
步骤 6：用户选择或继续迭代，登记 Research Brief 与 PICOS Profile 草案
```

**可并行步骤**：步骤 2 的热点检索、PICOS 草案和数据集摘要无直接依赖，可同时执行。  
**强制中断步骤**：步骤 3；研究方向会影响后续证据、设计和分析全链路，必须由用户确认。热点信息属于动态外部信息，必须展示来源和检索时间。

### 3. 信息来源

| 需要的信息 | 用户输入 | 系统接口 | 跨会话记忆 | 缺失处理 |
|---|:---:|:---:|:---:|---|
| 研究领域、疾病、偏好、目标期刊 | ✓ |  |  | 直接询问 |
| 用户历史研究领域、往期选题偏好 |  |  | ✓ | 无记录则忽略 |
| 历史上传数据集摘要 |  | ✓ | ✓ | 无数据仍可规划，但降低可行性评分 |
| 热点、资助与文献证据 |  | ✓ |  | 扩大时间或领域前先征求同意 |

### 4. 输出规范

1. **最终交付**：备选选题列表、标准化 PICOS、创新可行性评估、初步研究大纲、证据来源清单。
2. **语气风格**：专业严谨、临床医生可理解、结论优先；明确区分事实、推断和建议。
3. **用户操作**：需确认后继续；支持导出选题方案；被选方案登记为 Research Brief。
4. **禁止内容**：不得宣称未经证据支持的“首次”“填补空白”或承诺中标、发表。

### 5. 跨会话记忆

| 需长期记忆信息 | 记忆类型 | 示例 | 写入时机 |
|---|---|---|---|
| 长期深耕疾病领域、偏好研究类型 | user、feedback | 肺癌预后类真实世界研究 | 用户明确确认后 |
| 认可的选题风格 | feedback | 偏好稳妥、易落地、适合科研资助申请 | 用户选择方案后 |
| 当前项目选题、PICOS 与约束 | project | 研究人群、结局、数据条件 | 保存项目方案时 |

### 6. 异常与边界

| 异常情况 | Agent 处理方案 |
|---|---|
| 未提供研究领域 | 引导填写疾病、科室或临床问题；核心信息缺失时不启动热点检索 |
| 热点检索无匹配证据 | 展示当前检索边界，询问是否扩大年限或放宽领域，不自动扩大 |
| 三套方案全部不满意 | 追问创新不足、难度过高或数据不足等原因，保留反馈后重新生成 |
| PICOS 人群与干预冲突 | 标注冲突、说明影响、给出修改选项，修正后重新评分 |
| 外部热点信息过期或来源不明 | 标记不可用于正式论证，不纳入创新评分 |

### 7. 与其他任务衔接

1. **下一步**：F-02 文献综述、F-03 研究设计。
2. **触发方式**：询问用户是否继续，不自动启动。
3. **上游任务**：无，是科研主流程起点；也允许用户导入已有 Research Brief 直接开始。

---

## 任务 2：文献综述 Agent（F-02）

### 任务基础信息

- **任务名称**：文献综述任务
- **Agent 名称**：A-文献综述
- **一句话描述**：用户希望形成可发表、可复核的文献综述，Agent 帮其确定综述类型和协议，完成多库检索、筛选、质量评价、主题综合、引用核验与正式交付。

### 1. 工具清单

| 工具名称（英文） | 工具能力说明 | 需要哪些输入 | 返回什么 |
|---|---|---|---|
| `define_review_protocol` | 确定综述类型、研究问题、范围、纳排标准和评价方法 | Research Brief/PICOS、目标、时限 | Review Protocol、缺失项、协议风险 |
| `build_database_search_strategy` | 形成概念矩阵、主题词、同义词及逐库检索式 | Review Protocol、目标数据库 | Search Strategy、召回测试记录 |
| `search_literature_databases` | 按已确认策略并行检索多个数据库并记录执行日志 | 逐库检索式、时间范围、权限 | 候选题录、Search Log、失败源 |
| `import_literature_records` | 导入 PDF、DOI、PMID、RIS、BibTeX、EndNote | 文件或标识符 | 题录、全文解析状态、来源标识 |
| `deduplicate_literature_records` | 按 DOI、PMID、题名、年份和作者识别重复及多版本 | 合并题录 | 唯一题录、重复组、来源统计 |
| `screen_literature_records` | 执行标题摘要与全文筛选，记录排除原因和 PRISMA 计数 | 唯一题录、纳排标准 | Screening Set、PRISMA 数据、待裁决项 |
| `extract_study_evidence` | 提取设计、人群、样本、结局、效应、局限和原文定位 | 纳入全文、提取 Schema | Evidence Card、字段置信度、原文定位 |
| `assess_risk_of_bias` | 按 RoB 2、ROBINS-I、NOS、JBI、QUADAS-2、AMSTAR 2 等评价 | Evidence Card、研究设计、评价工具 | 领域判断、总体风险、支持依据 |
| `synthesize_literature_evidence` | 按主题、机制、技术路径或时间综合证据并解释异质性 | Evidence Set、研究问题、综合方法 | Synthesis Map、共识、异质性说明 |
| `detect_debate_and_gap` | 对比结论并识别争议与证据缺口 | 主题簇、PICOS | 争议矩阵、空白候选、证据强度 |
| `verify_review_citations` | 核验 DOI/PMID、题录元数据、撤稿状态和陈述—引用映射 | 稿件、Evidence Set、Reference Set | Citation Report、冲突与阻断项 |
| `compose_literature_review` | 按确认结构生成主题式综述正文与附录 | 协议、Evidence Set、Synthesis Map | Review Manuscript、引用映射 |
| `export_review_package` | 导出正文、证据矩阵、检索筛选日志和核验报告 | 已确认稿件与全部资产 | Review Package、manifest、审计记录 |

### 2. 执行流程

```text
步骤 1：读取 Research Brief/PICOS，调用 define_review_protocol 确定综述类型与方法边界
  ↓
步骤 2：调用 build_database_search_strategy 形成逐库检索式与召回测试
  ↓
步骤 3：强制中断 R1——展示 Review Protocol 与 Search Strategy，等待用户确认
  ↓
步骤 4：并行执行 search_literature_databases、import_literature_records，随后去重
  ↓
步骤 5：调用 screen_literature_records 完成标题摘要/全文筛选与 PRISMA 计数
  ↓
步骤 6：并行执行 extract_study_evidence 与 assess_risk_of_bias，校验 Evidence Set
  ↓
步骤 7：调用 synthesize_literature_evidence、detect_debate_and_gap 形成证据综合
  ↓
步骤 8：强制中断 R1——确认最终纳入集、质量分布、主题结构、争议与空白
  ↓
步骤 9：调用 compose_literature_review 与 verify_review_citations 生成并核验正文
  ↓
步骤 10：强制中断 R3——确认正式版本后调用 export_review_package
```

**可并行步骤**：多个数据库检索、文件导入、证据提取和质量评价可并行。  
**强制中断步骤**：步骤 3、8、10；分别冻结方法协议、证据范围与正式导出版本。

### 3. 信息来源

| 需要的信息 | 用户输入 | 系统接口 | 跨会话记忆 | 缺失处理 |
|---|:---:|:---:|:---:|---|
| 研究主题、PICOS | ✓ | ✓ | ✓ | 优先读取项目资产，冲突时询问用户 |
| 文献文件、DOI、题录 | ✓ | ✓ |  | 提示导入或授权检索 |
| 综述类型、检索范围与纳排标准 | ✓ |  | ✓ | 询问用户，不擅自把叙述综述当作系统评价 |
| 引文元数据 |  | ✓ |  | 缺失项标记待核验 |
| 质量评价工具与审阅者 | ✓ | ✓ |  | 按研究设计建议工具；不伪造双人审阅 |

### 4. 输出规范

1. **最终交付**：Review Protocol、Search Log、Screening Set、PRISMA 数据、Evidence Set、偏倚风险评价、Synthesis Map、Review Manuscript 与 Review Package。
2. **语气风格**：证据导向、克制，不把相关性写成因果，不隐藏矛盾研究。
3. **引用要求**：事实陈述绑定文献 ID 和原文位置；AI 综合与推断明确标注。
4. **用户操作**：确认纳入范围和结构；支持导出 Evidence Set 与综述文档。

### 5. 跨会话记忆

| 需长期记忆信息 | 记忆类型 | 示例 | 写入时机 |
|---|---|---|---|
| 常用数据库、语言和年限偏好 | feedback | PubMed + Web of Science、近 5 年、英文 | 用户确认检索策略后 |
| 当前项目协议、纳排标准与证据集 | project、reference | 系统评价；仅纳入成人队列研究 | 协议确认及保存 Evidence Set 时 |
| 用户偏好的综述结构 | feedback | 技术路径对比优先 | 用户选择结构后 |

### 6. 异常与边界

| 异常情况 | Agent 处理方案 |
|---|---|
| PDF 无法解析或加密 | 标记失败，提示提供可读文件或仅使用元数据，不伪造正文内容 |
| 文献元数据冲突 | 展示不同来源，优先 DOI/出版社信息并请求确认 |
| 计划数据库不可访问 | 记录数据库、错误、日期与替代来源；低于协议覆盖要求时标记 PARTIAL |
| 检索结果过多 | 建议收紧 PICOS、文献类型或时间范围，等待确认后重新检索 |
| 证据不足 | 明确说明证据等级和数量，不生成确定性结论 |
| 引用找不到原文支持 | 移除或标记待核验，禁止进入正式稿 |
| PRISMA 计数不闭合 | 阻断正式图表和导出，返回筛选日志修复 |
| 用户要求“AI 双人筛选” | 明确 AI 不得冒充第二名人工审阅者，记录真实操作者和 AI 辅助范围 |

### 7. 与其他任务衔接

1. **下一步**：F-03 研究设计、F-08 报告生成。
2. **触发方式**：询问用户是否将证据空白转化为研究设计，或将 Meta-analysis 计划交给 F-06。
3. **上游任务**：F-01 选题规划；也可由用户直接导入既有 PICOS 与文献开始。

---

## 任务 3：研究设计 Agent（F-03）

### 任务基础信息

- **任务名称**：研究设计任务
- **Agent 名称**：A-研究设计
- **一句话描述**：用户想要把临床研究问题转化为可执行、可审计的研究方案，Agent 帮其完成问题解析、设计路由、PICOS 校验、样本量计算、偏倚控制、访视/CRF、伦理数据治理与正式 Protocol 校验。

### 1. 工具清单

| 工具名称（英文） | 工具能力说明 | 需要哪些输入 | 返回什么 |
|---|---|---|---|
| `parse_clinical_research_question` | 从自然语言、Research Brief 或 Evidence Set 中解析研究目的、PICOS/PECO、时间轴和资源约束 | 研究问题、选题方案、证据空白、数据条件 | 结构化研究问题、缺失项、歧义项 |
| `classify_study_objective` | 判定诊断、疗效、病因、预后、预测、描述或真实世界比较等目标 | 结构化研究问题、用户偏好 | 研究目标类型、候选目标、置信度 |
| `route_study_design` | 基于目标、时间方向、分配方式和数据来源生成设计候选 | 研究目标、PICOS、数据来源、资源约束 | 设计候选、适用理由、排除理由、报告规范 |
| `validate_design_logic` | 检查时间顺序、对照、结局、选择机制、伦理和可行性冲突 | PICOS、设计候选、数据条件 | 冲突、阻断项、修改建议 |
| `define_eligibility_criteria` | 生成纳入排除标准并检查过宽/过窄和数据可采集性 | 人群定义、研究目标、数据字段 | 纳排标准、可执行性提示 |
| `define_intervention_exposure_control` | 明确干预/暴露/对照定义、窗口期、测量方式和分组规则 | PICOS、设计类型、数据字段 | 暴露/干预/对照操作定义 |
| `define_outcome_estimand` | 定义主要/次要结局、时间窗、估计目标和事件处理规则 | 研究目标、结局候选、随访窗口 | 结局定义、Estimand、测量规范 |
| `build_schedule_of_activities` | 生成筛选、基线、干预/暴露、随访、结局评价和质控访视表 | 设计类型、结局、随访周期 | Schedule of Activities |
| `calculate_sample_size` | 使用确定性计算器完成样本量/事件数估算与敏感性场景 | 设计类型、效应量、α、把握度、失访率 | 样本量报告、公式、参数来源 |
| `build_bias_control_plan` | 生成随机化、盲法、匹配、加权、分层、敏感性分析等偏倚控制方案 | 设计类型、变量、数据来源 | 偏倚清单、控制措施、残余风险 |
| `draft_statistical_analysis_framework` | 形成主分析、协变量、亚组、敏感性分析和方法交接框架 | 设计、结局、偏倚计划 | 统计分析框架、F-05 交接条件 |
| `generate_crf_schema` | 生成 CRF 字段、单位、编码、敏感级别和校验规则 | 访视表、结局、暴露、数据来源 | CRF Schema、字段字典 |
| `assess_ethics_and_data_governance` | 检查伦理路径、知情同意/豁免、脱敏、授权和注册要求 | 方案草案、数据来源、机构限制 | 伦理与数据治理清单、阻断项 |
| `compose_study_protocol` | 基于模板生成 Study Protocol、Synopsis 和附录 | 已确认设计、样本量、CRF、伦理清单 | Study Protocol 草案、导出清单 |
| `validate_study_protocol` | 校验正式方案章节一致性、必填项、状态和确认记录 | Protocol 草案、关联产物 | 校验报告、可批准/阻断状态 |

### 2. 执行流程

```text
步骤 1：读取 Research Brief、Evidence Set、已有 PICOS 或自然语言问题
  ↓
步骤 2：调用 parse_clinical_research_question、classify_study_objective
  ↓
步骤 3：调用 route_study_design、validate_design_logic 形成设计候选和阻断项
  ↓
步骤 4：强制中断——展示设计候选、关键假设、排除理由和残余风险，等待用户确认设计
  ↓
步骤 5：并行定义纳排标准、干预/暴露/对照、结局与估计目标
  ↓
步骤 6：生成访视表，收集样本量参数并调用 calculate_sample_size
  ↓
步骤 7：并行生成偏倚控制、统计分析框架、CRF Schema、伦理与数据治理清单
  ↓
步骤 8：强制中断——确认样本量参数、偏倚策略、CRF 与伦理阻断项
  ↓
步骤 9：调用 compose_study_protocol 与 validate_study_protocol
  ↓
步骤 10：用户确认后登记 PICOS Profile、Design Decision Record、Sample Size Report、Study Protocol、CRF Schema
```

**可并行步骤**：步骤 5 的纳排/暴露/结局定义可并行；步骤 7 的偏倚、统计框架、CRF、伦理清单可并行。  
**强制中断步骤**：步骤 4、步骤 8、步骤 10；研究设计、样本量参数、伦理阻断项和正式 Protocol 版本必须人工确认。样本量只能由确定性工具计算。

### 3. 信息来源

| 需要的信息 | 用户输入 | 系统接口 | 跨会话记忆 | 缺失处理 |
|---|:---:|:---:|:---:|---|
| 研究目的、PICOS/PECO、目标人群 | ✓ | ✓ | ✓ | 冲突时以当前项目确认值为准 |
| 数据来源、可用样本、事件数、随访周期 | ✓ | ✓ | ✓ | 直接询问或标记为可行性待确认 |
| 效应量、基线发生率、α、把握度、失访率 | ✓ | ✓ |  | 无依据时仅输出敏感性场景，不给唯一正式值 |
| 干预/暴露/对照和主要结局定义 | ✓ | ✓ | ✓ | 不明确时阻断样本量与正式方案 |
| 伦理、注册、数据授权、脱敏要求 | ✓ | ✓ | ✓ | 标记阻断项，不得批准方案 |

### 4. 输出规范

1. **最终交付**：PICOS Profile、Design Decision Record、Sample Size Report、Bias Control Plan、Statistical Analysis Framework、Schedule of Activities、CRF Schema、Ethics & Data Governance Checklist、Study Protocol。
2. **语气风格**：方法学严谨，结论先行；同时用临床医生能理解的语言解释专业术语。
3. **计算要求**：样本量展示方法 ID、公式、输入、单位、参数来源、失访/无效率调整、软件/脚本版本和敏感性场景。
4. **用户操作**：研究设计、样本量参数、伦理阻断项和正式导出必须确认；支持导出协议文档和附录。

### 5. 跨会话记忆

| 需长期记忆信息 | 记忆类型 | 示例 | 写入时机 |
|---|---|---|---|
| 用户常用研究设计与数据来源 | user、feedback | 回顾性队列、院内电子病历、真实世界比较 | 用户确认后 |
| 项目 PICOS、设计决策和样本量参数 | project | 双侧 α=0.05、把握度 80%、失访 10% | 方案确认时 |
| 机构固定伦理/CRF/脱敏约束 | reference、project | 不采集身份证号，日期相对化 | 用户或管理员确认后 |
| 被排除设计及原因 | project、feedback | RCT 因伦理/资源不可行被排除 | 设计确认时 |

### 6. 异常与边界

| 异常情况 | Agent 处理方案 |
|---|---|
| 研究目的与设计类型不匹配 | 解释冲突并提供替代设计，不继续样本量计算 |
| 主要结局或时间零点不明确 | 标记阻断项，要求先确认结局定义和观察窗口 |
| 效应量无可靠依据 | 给出多个敏感性场景，要求用户选择依据或转为可行性研究 |
| 可用样本量明显不足 | 展示把握度/事件数影响，建议调整结局、设计或扩大数据来源 |
| 伦理、注册或数据授权不明确 | 标记阻断项，不将方案描述为可直接执行 |
| 用户要求 Agent 直接批准最终方案 | 明确最终决定需研究者/统计师/伦理人员确认，保留候选状态 |
| 随机化、适应性、聚类或非劣效设计复杂 | 转统计师复核；简单公式只能作为初步估算 |

### 7. 与其他任务衔接

1. **下一步**：F-11 伦理注册、F-12 研究执行；如已有数据或回顾性数据可并行进入 F-04 数据清洗、F-05 算法推荐；复杂 Meta-analysis 协议转 F-02/F-06。
2. **触发方式**：方案确认后询问是否创建 Analysis Plan 或 Dataset Profile。
3. **上游任务**：F-01 选题规划、F-02 文献综述；也支持导入已有研究方案。

---

## 任务 4：数据清洗 Agent（F-04）

### 任务基础信息

- **任务名称**：数据清洗任务
- **Agent 名称**：A-数据清洗
- **一句话描述**：用户想要得到适合分析的数据，Agent 帮其理解字段语义、识别质量问题、生成可解释清洗计划，并在确认后调用确定性执行器生成新数据版本。

### 1. 工具清单

| 工具名称（英文） | 工具能力说明 | 需要哪些输入 | 返回什么 |
|---|---|---|---|
| `inspect_dataset_schema` | 读取字段类型、标签、单位、编码和敏感级别 | Dataset Version ID | 数据字典、字段警告 |
| `profile_data_quality` | 统计缺失、分布、唯一性、重复和异常概况 | Dataset Version ID | 数据质量报告 |
| `detect_data_issues` | 按规则识别范围、逻辑、时间和跨字段冲突 | 数据版本、规则集 | 问题清单、影响范围 |
| `recommend_cleaning_plan` | 结合研究方案或分析目标提出清洗、插补、变换和排除候选 | 质量报告、Study Protocol/分析目标 | Cleaning Plan、理由、风险 |
| `preview_cleaning_impact` | 预估规则对行、字段、样本量和事件数的影响 | Cleaning Plan、数据版本 | 影响摘要、不可逆风险 |
| `execute_cleaning_plan` | 在隔离执行器中应用已批准规则 | 批准计划、Dataset Version ID | 新数据版本、差异摘要、日志 |

### 2. 执行流程

```text
步骤 1：确认数据授权、用途、Study Protocol 或分析目标，读取数据字典
  ↓
步骤 2：并行调用 profile_data_quality、detect_data_issues
  ↓
步骤 3：调用 recommend_cleaning_plan 形成规则候选
  ↓
步骤 4：调用 preview_cleaning_impact 展示受影响行、字段、样本量和事件数
  ↓
步骤 5：强制中断——用户逐项确认、修改或拒绝清洗规则
  ↓
步骤 6：调用 execute_cleaning_plan，生成新的 Dataset Version
  ↓
步骤 7：对新版本重新画像，交付前后差异和完整日志
```

**可并行步骤**：缺失、异常和逻辑规则检测可并行。  
**强制中断步骤**：步骤 5；应用清洗属于 R2 数据变更。原始数据始终只读，Agent 无权覆盖。

### 3. 信息来源

| 需要的信息 | 用户输入 | 系统接口 | 跨会话记忆 | 缺失处理 |
|---|:---:|:---:|:---:|---|
| 数据版本、字典和授权 | ✓ | ✓ | ✓ | 无授权或版本不明时终止 |
| 字段临床含义、单位、合法范围 | ✓ | ✓ | ✓ | 无法判断时询问，不按常识擅改 |
| 研究方案、分析目标与关键变量 |  | ✓ | ✓ | 无计划可做通用画像，但不做目标性删除 |
| 机构清洗规则 |  | ✓ | ✓ | 记录规则版本和来源 |

### 4. 输出规范

1. **最终交付**：质量报告、Cleaning Plan、影响预览、新 Dataset Version、差异摘要、可复现脚本和执行日志。
2. **语气风格**：明确风险，避免把“异常”直接等同于“错误”。
3. **数据要求**：原始版本不可变；每条修改可追踪到规则和确认人。
4. **用户操作**：清洗规则逐项确认；支持回滚到任一历史版本。

### 5. 跨会话记忆

| 需长期记忆信息 | 记忆类型 | 示例 | 写入时机 |
|---|---|---|---|
| 用户/机构常用编码与单位 | user、reference | 吸烟史：0/1；肌酐：μmol/L | 明确确认后 |
| 项目数据字典和清洗规则 | project | 年龄范围 18～90 | 保存计划时 |
| 用户对异常和插补的偏好 | feedback | 默认不删除极端值，先人工核验 | 用户明确表达后 |

### 6. 异常与边界

| 异常情况 | Agent 处理方案 |
|---|---|
| 数据文件损坏或格式不支持 | 终止执行，提供支持格式和修复建议 |
| 字段单位或编码不明 | 标记阻断字段，询问用户，不自动转换 |
| 清洗导致样本量/事件数大幅下降 | 高亮影响并建议替代方案，必须二次确认 |
| 缺失机制无法判断 | 展示 MCAR/MAR/MNAR 假设差异，不把单一插补作为默认真解 |
| 执行器失败 | 保留失败日志，不产生新数据版本，允许修正计划后重试 |

### 7. 与其他任务衔接

1. **下一步**：F-05 重新评估算法适配，或 F-06 执行已批准分析计划。
2. **触发方式**：新数据版本复核后询问用户是否继续。
3. **上游任务**：F-03 研究设计、F-05 算法推荐或用户直接上传数据。

---

## 任务 5：算法推荐 Agent（F-05）

### 任务基础信息

- **任务名称**：算法推荐任务
- **Agent 名称**：A-算法推荐
- **一句话描述**：用户想要为研究目标选对统计方法，Agent 帮其结合已确认研究设计、数据画像和分析目标，输出受规则约束且可解释的算法方案。

### 1. 工具清单

| 工具名称（英文） | 工具能力说明 | 需要哪些输入 | 返回什么 |
|---|---|---|---|
| `profile_dataset` | 生成变量类型、分布、缺失、样本量与事件数画像 | Dataset Version ID | 数据画像、质量警告 |
| `query_algorithm_registry` | 查询当前已验证、可执行的算法与版本 | 研究设计、分析目标 | 算法候选、状态、输入要求 |
| `filter_method_constraints` | 用硬规则排除不适用方法 | PICOS、数据画像、算法候选 | 可用集合、排除原因 |
| `evaluate_method_fit` | 对方法假设、样本适配和输出需求评分 | 可用集合、数据画像、用户目标 | 推荐排序、适配评分、风险 |
| `get_algorithm_documentation` | 获取方法说明、限制、替代方案与预期输出 | Algorithm Version ID | 方法卡片、诊断项、输出字段 |
| `generate_analysis_plan` | 将用户选择转成确定的变量与参数计划 | 选定算法、变量映射、参数 | Analysis Plan 草案 |

### 2. 执行流程

```text
步骤 1：读取已确认 PICOS/Study Protocol，询问主分析目标与次要目标
  ↓
步骤 2：并行调用 profile_dataset、query_algorithm_registry
  ↓
步骤 3：调用 filter_method_constraints 和 evaluate_method_fit
  ↓
步骤 4：强制中断——展示主推荐、备选、排除原因、实现状态和预期输出
  ↓
步骤 5：用户选择方法并确认变量映射、参数和分层策略
  ↓
步骤 6：调用 generate_analysis_plan，保存待批准 Analysis Plan
```

**可并行步骤**：数据画像和算法注册表查询可并行。  
**强制中断步骤**：步骤 4、5；Agent 不能替用户做最终统计方法决策，也不能静默替换未实现方法。

### 3. 信息来源

| 需要的信息 | 用户输入 | 系统接口 | 跨会话记忆 | 缺失处理 |
|---|:---:|:---:|:---:|---|
| PICOS、研究设计、分析目标 | ✓ | ✓ | ✓ | 缺少确认版设计时返回 F-03 |
| 数据画像与变量字典 |  | ✓ |  | 无数据时只给设计级候选并标记待验证 |
| 算法状态、版本和限制 |  | ✓ |  | 仅信任算法注册表当前状态 |
| 用户历史方法偏好 |  |  | ✓ | 仅用于解释顺序，不能覆盖硬约束 |

### 4. 输出规范

1. **最终交付**：方法推荐卡、排除清单、假设与风险、预期输出、Analysis Plan 草案。
2. **语气风格**：结论优先，给出“为什么适合/为什么不适合”。
3. **透明要求**：显示算法版本、可用状态和数据要求；区分主分析、敏感性和探索性分析。
4. **用户操作**：选择方法并确认变量、参数后才可进入执行。

### 5. 跨会话记忆

| 需长期记忆信息 | 记忆类型 | 示例 | 写入时机 |
|---|---|---|---|
| 用户常用输出和解释深度 | feedback | 优先报告调整后 HR 与 95% CI | 用户确认偏好后 |
| 项目方法选择、变量映射和理由 | project | Cox；time=OS_month；event=death | Analysis Plan 保存时 |
| 被拒绝方案及原因 | project、feedback | 不使用 PSM：事件数不足 | 用户明确拒绝时 |

### 6. 异常与边界

| 异常情况 | Agent 处理方案 |
|---|---|
| 数据与设计冲突 | 标出冲突字段并阻断计划生成，建议修正数据或设计 |
| 推荐方法尚未实现 | 明确标记不可执行，给出已验证替代或建设状态 |
| 所有方法均不满足硬约束 | 不强行推荐；列出缺口并建议补数据、改目标或咨询统计师 |
| 用户坚持明显不适用方法 | 记录警告和理由，禁止进入正式主分析；可在合规允许时标记探索性 |
| 算法注册表不可用 | 终止推荐，不使用模型记忆猜测执行能力 |

### 7. 与其他任务衔接

1. **下一步**：F-06 统计分析执行；若数据质量不足则先进入 F-04。
2. **触发方式**：用户批准 Analysis Plan 后询问是否执行。
3. **上游任务**：F-03 研究设计、F-04 数据清洗或用户导入的既有方案。

---

## 任务 6：统计分析执行 Agent（F-06）

### 任务基础信息

- **任务名称**：统计分析执行任务
- **Agent 名称**：A-统计分析
- **一句话描述**：用户想要根据已批准分析计划获得可信统计结果，Agent 帮其校验输入与方法假设、编排确定性执行器、处理诊断异常并解释已验证结果。

### 1. 工具清单

| 工具名称（英文） | 工具能力说明 | 需要哪些输入 | 返回什么 |
|---|---|---|---|
| `validate_analysis_plan` | 校验数据版本、变量映射、参数和算法版本 | Analysis Plan、Dataset Version | 校验结果、阻断项、警告 |
| `check_model_assumptions` | 执行正态性、比例风险、共线性等前提检查 | 数据版本、算法、变量 | 假设诊断、建议处理 |
| `estimate_run_cost` | 估算运行时间、资源和输出范围 | 算法版本、数据规模、参数 | 成本预估、超时风险 |
| `execute_statistical_model` | 调用隔离 Python/R Worker 真实执行算法 | 已批准计划、固定数据版本 | Analysis Run ID、原始输出、日志 |
| `validate_result_bundle` | 检查 Schema、范围、收敛和统计量一致性 | Analysis Run 输出 | 校验报告、Result Bundle |
| `explain_verified_result` | 用临床可理解语言解释已验证结果与限制 | Verified Result、研究设计 | 结果摘要、限制、禁止性表述提示 |

### 2. 执行流程

```text
步骤 1：读取已批准 Analysis Plan、Dataset Version 与算法版本
  ↓
步骤 2：并行调用 validate_analysis_plan、check_model_assumptions、estimate_run_cost
  ↓
步骤 3：若存在可修正警告，Agent 提出候选方案；若存在阻断项则停止
  ↓
步骤 4：强制中断——展示实际执行参数、资源预估、假设状态和风险，等待用户确认
  ↓
步骤 5：调用 execute_statistical_model 创建不可变 Analysis Run
  ↓
步骤 6：调用 validate_result_bundle；需要人工复核时进入 REVIEWED 阶段
  ↓
步骤 7：结果通过后登记 Verified Result，再调用 explain_verified_result
```

**可并行步骤**：输入校验、假设检查和成本估算可并行。  
**强制中断步骤**：步骤 4；启动分析属于 R2 计算操作。任何模型调整均形成新 Analysis Plan/Run，不覆盖旧运行。

### 3. 信息来源

| 需要的信息 | 用户输入 | 系统接口 | 跨会话记忆 | 缺失处理 |
|---|:---:|:---:|:---:|---|
| 已批准 Analysis Plan | ✓ | ✓ | ✓ | 缺少批准状态时返回 F-05 |
| 固定 Dataset Version |  | ✓ | ✓ | 版本不明确时禁止执行 |
| 算法镜像、版本和限制 |  | ✓ |  | 仅从算法注册表读取 |
| 资源预算与超时限制 | ✓ | ✓ | ✓ | 使用项目上限并展示给用户 |

### 4. 输出规范

1. **最终交付**：Analysis Run、标准化 Result Bundle、假设诊断、Verified Result、可复现代码、环境和日志。
2. **语气风格**：先报告结果，再报告适用边界；区分统计显著、临床意义和因果解释。
3. **数值要求**：所有数值由执行器输出；解释文本以 result ID 引用，不复制无来源数字。
4. **用户操作**：执行前确认；正式结果可设置统计复核者批准。

### 5. 跨会话记忆

| 需长期记忆信息 | 记忆类型 | 示例 | 写入时机 |
|---|---|---|---|
| 用户偏好的结果展示方式 | feedback | 效应量 + 95% CI 优先于只报 P 值 | 用户确认偏好后 |
| 项目运行、参数、诊断和结果引用 | project | run_456、algorithm_v2.3、dataset_v4 | 每次运行完成后 |
| 被否决模型与原因 | project | 比例风险假设不满足，改用时变系数 | 用户批准调整时 |

### 6. 异常与边界

| 异常情况 | Agent 处理方案 |
|---|---|
| 变量缺失或类型错误 | 阻断运行，给出变量映射修正建议 |
| 模型不收敛或假设严重违规 | 保留失败运行，解释原因，提出备选但不自动改模 |
| Worker 超时或崩溃 | 记录技术失败，不登记结果；允许按重试策略重跑 |
| 结果极端或与预期相反 | 不篡改结果；启动数据、模型和敏感性检查建议 |
| 用户要求修改 P 值或删除不显著结果 | 拒绝修改，保留完整结果和分析透明度 |

### 7. 与其他任务衔接

1. **下一步**：F-07 统计绘图、F-08 报告生成；必要时返回 F-05/F-04 修订计划或数据。
2. **触发方式**：Verified Result 形成后询问用户选择图表或报告。
3. **上游任务**：F-05 算法推荐、F-04 数据清洗。

---

## 任务 7：统计绘图 Agent（F-07）

### 任务基础信息

- **任务名称**：统计绘图任务
- **Agent 名称**：A-统计绘图
- **一句话描述**：用户想要将真实分析结果变成发表级图表，Agent 帮其匹配图形类型、生成可执行绘图规范与代码、优化布局并导出可复现图表包。

### 1. 工具清单

| 工具名称（英文） | 工具能力说明 | 需要哪些输入 | 返回什么 |
|---|---|---|---|
| `recommend_plot_spec` | 根据结果类型和表达目标推荐图形 | Verified Result、展示目标 | 图形候选、适用理由、Plot Spec |
| `load_journal_plot_style` | 读取目标期刊尺寸、字体、色彩和格式规则 | 期刊 ID、图件类型 | 版本化样式模板、更新时间 |
| `generate_plot_code` | 基于结果引用和 Plot Spec 生成 Python/R 代码 | Plot Spec、result refs | 可执行代码、依赖清单 |
| `render_scientific_figure` | 在隔离 Worker 中渲染图表 | 代码、固定结果、环境 | 预览图、渲染日志、Figure Asset |
| `optimize_figure_layout` | 调整标签重叠、图例、留白与可读性 | Figure Asset、布局约束 | 新布局版本、变更摘要 |
| `export_figure_bundle` | 导出 SVG/PDF/PNG、代码与 manifest | 已确认 Figure Asset | 图表包、文件哈希、结果引用 |

### 2. 执行流程

```text
步骤 1：选择 Verified Result，收集目标期刊、图形用途和风格偏好
  ↓
步骤 2：并行调用 recommend_plot_spec、load_journal_plot_style
  ↓
步骤 3：强制中断——展示图形候选、数据映射、尺寸和视觉编码，等待确认
  ↓
步骤 4：调用 generate_plot_code、render_scientific_figure 生成首版预览
  ↓
步骤 5：调用 optimize_figure_layout 或按用户反馈迭代样式
  ↓
步骤 6：确认最终图后调用 export_figure_bundle
```

**可并行步骤**：图形推荐和期刊样式读取可并行；多个独立图件可并行渲染。  
**强制中断步骤**：步骤 3 和最终导出前；视觉编码会影响结果理解，必须确认。布局优化不得修改统计数据。

### 3. 信息来源

| 需要的信息 | 用户输入 | 系统接口 | 跨会话记忆 | 缺失处理 |
|---|:---:|:---:|:---:|---|
| Verified Result 和 result refs |  | ✓ | ✓ | 非验证结果只能生成“草稿”水印图 |
| 目标期刊与图件要求 | ✓ | ✓ | ✓ | 无目标期刊时使用通用出版模板 |
| 颜色、语言、尺寸偏好 | ✓ |  | ✓ | 使用无障碍通用默认值并允许修改 |
| 期刊样式版本 |  | ✓ |  | 缺失时提示人工核对，不虚构规则 |

### 4. 输出规范

1. **最终交付**：SVG、PDF、PNG、绘图代码、依赖环境、Plot Spec、结果引用 manifest。
2. **语气风格**：说明图形选择理由、视觉映射和潜在误读风险。
3. **一致性要求**：图中数据、注释和统计量与 Verified Result 一致；重新运行可得到等价图表。
4. **用户操作**：确认图形类型和最终版；支持对话式调样式，但不得通过视觉手段夸大效应。

### 5. 跨会话记忆

| 需长期记忆信息 | 记忆类型 | 示例 | 写入时机 |
|---|---|---|---|
| 用户常用图表语言和风格 | feedback | 英文标签、无边框、色盲友好 | 用户确认后 |
| 项目期刊模板与 Figure Asset | project | NEJM 风格、双栏宽度 | 保存图件时 |
| 已批准的颜色语义 | project、reference | 暴露组酒红，对照组灰蓝 | 用户确认后 |

### 6. 异常与边界

| 异常情况 | Agent 处理方案 |
|---|---|
| 结果类型不适合所选图形 | 解释误导风险并推荐替代，不直接渲染正式图 |
| 标签严重重叠 | 调整布局、筛选标注规则或建议分面，保留原始点位 |
| 期刊模板过期 | 显示更新时间，要求用户核对最新 author guideline |
| 渲染代码失败 | 展示依赖/语法错误，修复后产生新渲染版本 |
| 用户要求隐藏不利数据点 | 拒绝选择性隐藏；允许采用透明、可解释的预设过滤规则 |

### 7. 与其他任务衔接

1. **下一步**：F-08 报告生成。
2. **触发方式**：图件确认后询问是否加入稿件上下文。
3. **上游任务**：F-06 统计分析执行。

---

## 任务 8：报告生成 Agent（F-08）

### 任务基础信息

- **任务名称**：报告生成任务
- **Agent 名称**：A-报告生成
- **一句话描述**：用户想要将研究方案、证据和真实结果组织成规范稿件，Agent 帮其汇集已验证产物、生成 IMRAD 章节和统计表，并在溯源与合规校验后导出。

### 1. 工具清单

| 工具名称（英文） | 工具能力说明 | 需要哪些输入 | 返回什么 |
|---|---|---|---|
| `collect_verified_artifacts` | 汇集 PICOS、方案、证据、结果与图表 | Project ID、资产范围 | Manuscript Context、缺失清单 |
| `select_manuscript_template` | 匹配 IMRAD、病例报告等文章结构 | 研究设计、文章类型、期刊 | 章节 Schema、字数和必备项 |
| `draft_imrad_section` | 按章节和可用证据生成草稿 | 章节、Manuscript Context | 章节草稿、资产引用 |
| `generate_statistical_tables` | 从 Verified Result 生成 Table 1、回归表等 | Result refs、模板 | 表格资产、结果引用 |
| `validate_result_citations` | 核对稿件数值、图表和引文来源 | Manuscript Version | 溯源报告、阻断项 |
| `check_manuscript_compliance` | 检查结构、字数、报告规范和期刊要求 | 稿件、研究设计、期刊规则 | 合规报告、修改建议 |
| `export_manuscript` | 导出 Word、LaTeX、PDF 与项目 manifest | 合规通过稿件、格式 | 正式文稿包、导出审计记录 |

### 2. 执行流程

```text
步骤 1：选择文章类型、目标期刊和需要引用的项目资产
  ↓
步骤 2：并行调用 collect_verified_artifacts、select_manuscript_template
  ↓
步骤 3：强制中断——展示可用资产、缺失证据、章节计划与字数分配
  ↓
步骤 4：按用户选择逐章调用 draft_imrad_section，并并行生成统计表
  ↓
步骤 5：用户逐章审阅；修订形成新的 Manuscript Version
  ↓
步骤 6：调用 validate_result_citations、check_manuscript_compliance
  ↓
步骤 7：所有阻断项清零且用户强确认后，调用 export_manuscript
```

**可并行步骤**：资产汇集与模板读取、独立章节草稿、多个统计表可并行。  
**强制中断步骤**：步骤 3、逐章定稿和步骤 7；正式导出属于 R3。统计数值溯源覆盖率必须达到 100%。

### 3. 信息来源

| 需要的信息 | 用户输入 | 系统接口 | 跨会话记忆 | 缺失处理 |
|---|:---:|:---:|:---:|---|
| PICOS、方案、证据、结果和图表 | ✓ | ✓ | ✓ | 缺失资产明确列出，不用模型补造 |
| 文章类型、目标期刊和叙述重点 | ✓ | ✓ | ✓ | 直接询问或使用通用模板 |
| 统计数值与引用 |  | ✓ |  | 只接受 Verified Result/Evidence Set |
| 期刊与报告规范 |  | ✓ |  | 显示规则版本和更新时间 |

### 4. 输出规范

1. **最终交付**：IMRAD 稿件、统计表、图注、参考文献、溯源报告、合规报告、Word/LaTeX/PDF 和项目 manifest。
2. **语气风格**：学术克制、逻辑清晰，不夸大因果或临床价值。
3. **溯源要求**：每个统计数值绑定 result ID，每个事实引用绑定 evidence ID。
4. **用户操作**：章节级确认；正式导出必须强确认并通过合规闸门。

### 5. 跨会话记忆

| 需长期记忆信息 | 记忆类型 | 示例 | 写入时机 |
|---|---|---|---|
| 用户常用语言和学术表达偏好 | feedback | 英文、结果先行、避免过度形容 | 用户明确选择后 |
| 项目 Manuscript Version 与资产引用 | project | manuscript_v6 引用 res_789 | 每次保存稿件时 |
| 目标期刊和模板版本 | project、reference | 目标 JAMA，规则更新于某日 | 用户确认期刊后 |

### 6. 异常与边界

| 异常情况 | Agent 处理方案 |
|---|---|
| 稿件出现无来源统计数值 | 标记并阻断正式导出，要求绑定结果或删除 |
| 文献无法支持当前表述 | 降级表述、替换证据或标记待核验 |
| Results 与 Methods 不一致 | 列出冲突，要求修订方案描述或结果范围 |
| 用户要求生成不存在的结果 | 拒绝并建议回到 F-06 完成真实分析 |
| 期刊规则不完整或过期 | 生成通用稿件并显著提示人工核对，不宣称完全合规 |

### 7. 与其他任务衔接

1. **下一步**：F-09 文献格式化、F-10 期刊推荐。
2. **触发方式**：稿件达到可审阅状态后询问用户下一步。
3. **上游任务**：F-02 文献综述、F-03 研究设计、F-06 统计分析、F-07 统计绘图。

---

## 任务 9：文献格式化 Agent（F-09）

### 任务基础信息

- **任务名称**：文献格式化任务
- **Agent 名称**：A-文献格式化
- **一句话描述**：用户想要将题录批量转换成目标期刊规范，Agent 帮其解析题录、修复可验证缺失字段、处理作者消歧，并输出格式化结果与完整性报告。

### 1. 工具清单

| 工具名称（英文） | 工具能力说明 | 需要哪些输入 | 返回什么 |
|---|---|---|---|
| `import_reference_set` | 导入 BibTeX、EndNote、RIS 或稿件引文 | 文件、Manuscript ID | Reference Set、解析错误 |
| `resolve_reference_metadata` | 用 DOI、PMID 或出版社源补充元数据 | 题录标识符 | 候选元数据、来源、置信度 |
| `disambiguate_author` | 处理重名、缩写和作者顺序冲突 | 作者信息、ORCID 等 | 候选作者、冲突说明 |
| `load_journal_reference_style` | 加载期刊引文和参考文献规则 | Journal ID、规则版本 | 样式规则、更新时间 |
| `format_reference_set` | 按规则转换正文引文与文后列表 | Reference Set、样式规则 | 格式化引文、变更摘要 |
| `validate_reference_set` | 检查缺失字段、重复、未引用和顺序 | 格式化结果、稿件 | 完整性报告、阻断项 |

### 2. 执行流程

```text
步骤 1：导入题录或读取 Manuscript 引用，确认目标期刊
  ↓
步骤 2：并行调用 resolve_reference_metadata、disambiguate_author、load_journal_reference_style
  ↓
步骤 3：强制中断——展示元数据冲突、推断字段和低置信度项，等待用户确认
  ↓
步骤 4：调用 format_reference_set 完成格式转换
  ↓
步骤 5：调用 validate_reference_set 检查完整性和正文对应关系
  ↓
步骤 6：用户确认后保存新 Reference Set/Manuscript Version
```

**可并行步骤**：多条题录解析、作者消歧和期刊规则加载可并行。  
**强制中断步骤**：步骤 3；任何推断或冲突元数据必须确认，不能静默补造。

### 3. 信息来源

| 需要的信息 | 用户输入 | 系统接口 | 跨会话记忆 | 缺失处理 |
|---|:---:|:---:|:---:|---|
| 题录、正文引文、目标期刊 | ✓ | ✓ | ✓ | 缺少期刊时先用通用样式草稿 |
| DOI、PMID、ORCID 等标识 | ✓ | ✓ |  | 无可靠标识则标记人工核验 |
| 期刊规则 |  | ✓ | ✓ | 显示版本与更新时间 |
| 用户已确认的消歧结果 |  |  | ✓ | 仅复用于同一明确作者实体 |

### 4. 输出规范

1. **最终交付**：格式化正文引文、文后参考文献、缺失字段、冲突、重复和未引用报告。
2. **语气风格**：简洁、表格化展示问题，突出需要人工处理的少数项。
3. **数据要求**：每个补充字段保留来源与置信度；未经确认的推断不得进入正式稿。
4. **用户操作**：确认冲突和低置信度字段；支持导出 BibTeX/EndNote/RIS。

### 5. 跨会话记忆

| 需长期记忆信息 | 记忆类型 | 示例 | 写入时机 |
|---|---|---|---|
| 用户常用引文格式 | feedback | Vancouver | 用户确认后 |
| 项目 Reference Set 与消歧结果 | project、reference | Smith J 对应 ORCID ... | 人工确认后 |
| 目标期刊规则版本 | project、reference | JAMA style 某版本 | 保存格式化结果时 |

### 6. 异常与边界

| 异常情况 | Agent 处理方案 |
|---|---|
| 无 DOI 且元数据不完整 | 保留缺失标记，要求人工补充，不猜测卷页 |
| 多来源元数据冲突 | 并列展示来源和差异，等待选择 |
| 作者无法唯一消歧 | 保持原始信息并标记，不自动合并 |
| 期刊规则更新不明 | 提示核对官网最新指南，当前输出标记规则日期 |
| 正文引用与文后列表不一致 | 自动生成差异清单，修复前阻止正式导出 |

### 7. 与其他任务衔接

1. **下一步**：F-10 期刊推荐或返回 F-08 完成最终导出。
2. **触发方式**：格式校验通过后询问用户。
3. **上游任务**：F-02 文献综述、F-08 报告生成。

---

## 任务 10：期刊推荐 Agent（F-10）

### 任务基础信息

- **任务名称**：期刊推荐任务
- **Agent 名称**：A-期刊推荐
- **一句话描述**：用户想要为稿件选择合适投稿目标，Agent 帮其提取稿件画像、检索最新期刊规则、评估范围与要求匹配度，并形成分梯队投稿方案。

### 1. 工具清单

| 工具名称（英文） | 工具能力说明 | 需要哪些输入 | 返回什么 |
|---|---|---|---|
| `extract_manuscript_profile` | 提取主题、研究设计、创新点、篇幅和图表特征 | Manuscript Version | 稿件画像、关键词、文章类型 |
| `understand_submission_preference` | 解析分区、开放获取、周期、费用等自然语言约束 | 用户偏好 | 结构化硬约束与软偏好 |
| `query_journal_database` | 检索 scope、文章类型和最新投稿规则 | 稿件画像、约束 | 候选期刊、来源、更新时间 |
| `rank_journal_fit` | 从主题、设计、文章类型和约束多维排序 | 候选期刊、稿件画像 | 匹配分、排序、分项理由 |
| `assess_submission_risk` | 识别超字数、图表、研究类型和政策不匹配风险 | 稿件、期刊规则 | 风险清单、修改成本 |
| `generate_submission_plan` | 形成冲刺、匹配、保守三梯队方案 | 排序、风险、用户偏好 | 投稿梯队、准备清单、备选路径 |

### 2. 执行流程

```text
步骤 1：读取最新 Manuscript Version，收集分区、OA、周期、费用和地域偏好
  ↓
步骤 2：并行调用 extract_manuscript_profile、understand_submission_preference
  ↓
步骤 3：调用 query_journal_database、rank_journal_fit、assess_submission_risk
  ↓
步骤 4：强制中断——展示候选来源、匹配理由、风险、费用和规则更新时间
  ↓
步骤 5：用户调整偏好或选择候选期刊
  ↓
步骤 6：调用 generate_submission_plan 输出三梯队投稿方案与准备清单
```

**可并行步骤**：稿件画像与偏好解析可并行；候选期刊的规则和风险可并行检查。  
**强制中断步骤**：步骤 4、5；期刊选择属于重要外部决策。动态指标与规则必须显示来源和更新时间。

### 3. 信息来源

| 需要的信息 | 用户输入 | 系统接口 | 跨会话记忆 | 缺失处理 |
|---|:---:|:---:|:---:|---|
| 最新稿件、文章类型、研究设计 | ✓ | ✓ | ✓ | 无完整稿件时只做初步匹配 |
| 分区、OA、周期、预算等偏好 | ✓ |  | ✓ | 询问用户；软偏好可为空 |
| 期刊 scope、政策、费用与要求 |  | ✓ |  | 无法验证时不进入正式推荐 |
| 用户历史投稿和拒稿反馈 | ✓ |  | ✓ | 仅用于避免重复问题，不推断录用率 |

### 4. 输出规范

1. **最终交付**：候选期刊卡、匹配评分、来源与更新时间、风险、冲刺/匹配/保守三梯队和投稿准备清单。
2. **语气风格**：简洁务实，说明不确定性，不制造“必中”预期。
3. **信息要求**：动态指标、费用和政策标明来源日期；区分硬约束和软偏好。
4. **用户操作**：选择期刊后才生成具体改稿和投稿清单；不自动对外投稿。

### 5. 跨会话记忆

| 需长期记忆信息 | 记忆类型 | 示例 | 写入时机 |
|---|---|---|---|
| 分区、OA、周期和费用偏好 | user、feedback | Q1 优先、非 OA、周期小于 3 个月 | 用户确认后 |
| 项目目标期刊与备选梯队 | project | 冲刺 A、匹配 B、保守 C | 用户选择后 |
| 历史投稿结果和反馈 | project、feedback | 某刊因 scope 不符拒稿 | 用户主动记录后 |

### 6. 异常与边界

| 异常情况 | Agent 处理方案 |
|---|---|
| 用户约束互相冲突 | 列出冲突并要求排序，不静默放宽硬约束 |
| 无高匹配候选 | 说明原因，建议改稿、改变文章类型或放宽限制 |
| 期刊信息无法验证 | 移出正式推荐或标记待人工核对 |
| 用户询问保证录用概率 | 明确无法保证，只提供可解释匹配和风险评估 |
| 疑似掠夺性期刊 | 高风险警告，展示识别依据并默认排除 |

### 7. 与其他任务衔接

1. **下一步**：返回 F-08/F-09 按目标期刊修订稿件和引文；生成投稿准备清单。
2. **触发方式**：询问用户是否按选定期刊启动适配，不自动外发。
3. **上游任务**：F-08 报告生成、F-09 文献格式化。

---

## 任务 11：伦理注册 Agent（F-11）

### 1. 任务基础信息

- **任务名称**：伦理注册任务
- **一句话描述**：用户已形成研究方案，需要准备伦理审查、知情同意、隐私治理和临床注册材料，Agent 帮其判断审查路径、生成材料清单和注册字段草案。

### 2. 工具清单

| 工具名称 | 工具能力说明 | 需要哪些输入 | 返回什么 |
|---|---|---|---|
| `classify_ethics_review_type` | 判断豁免、快速审查、完整审查、前瞻性注册等路径 | Protocol、研究类型、干预风险、数据来源 | 伦理路径、风险等级、阻断项 |
| `generate_ethics_submission_checklist` | 生成伦理递交材料清单 | Protocol、CRF、机构模板 | 材料清单、缺失项、责任人建议 |
| `draft_informed_consent` | 生成知情同意书草案 | 研究目的、流程、风险、受益、隐私规则 | Consent Draft、需人工确认项 |
| `prepare_trial_registration_fields` | 生成临床注册字段草案 | Protocol、PICOS、主要/次要结局、招募计划 | Registration Draft |
| `check_registration_consistency` | 检查注册信息与方案一致性 | Protocol、Registration Draft | 不一致字段、修改建议 |
| `export_ethics_package` | 导出伦理与注册材料包 | 已确认清单、Consent、Registration Draft | Ethics Package、导出日志 |

### 3. 执行流程

步骤 1：读取已确认 Protocol、CRF、数据来源、机构伦理模板和用户目标。  
步骤 2：调用 `classify_ethics_review_type` 判断伦理路径、注册要求和阻断项。  
步骤 3：并行生成伦理递交清单、知情同意草案、隐私治理要点和注册字段草案。  
步骤 4：强制中断——展示伦理路径、缺失材料、注册字段和需人工确认项。  
步骤 5：调用 `check_registration_consistency` 校验注册字段与 Protocol 一致性。  
步骤 6：用户确认后调用 `export_ethics_package` 导出伦理材料包。

**强制中断步骤**：步骤 4、步骤 6；伦理路径、知情同意、注册字段和正式导出均属于 R2/R3。  
**边界**：Agent 不替代伦理委员会审批，不生成虚假伦理批准号或注册号，不自动提交外部伦理/注册平台。

### 4. 输出规范

1. **最终交付内容**：Ethics Package、Consent Draft、Privacy Checklist、Registration Draft、材料缺失清单。
2. **语气风格**：合规审慎、清单化、阻断项前置。
3. **用户操作要求**：伦理路径、注册字段、知情同意和正式导出必须确认。

### 5. 与其他任务衔接

1. **下一步**：F-12 研究执行、F-04 数据清洗、F-08 伦理声明。
2. **触发方式**：用户已有 Protocol 或 F-03 输出 Study Protocol 后触发。
3. **上游任务**：F-03 研究设计；必要时回到 F-02 补证据依据。

---

## 任务 12：研究执行 Agent（F-12）

### 1. 任务基础信息

- **任务名称**：研究执行管理任务
- **一句话描述**：用户进入研究执行期后，Agent 帮其管理招募、入组、随访、CRF 质控、方案偏离和监查报告，并将可分析数据交接给数据清洗与统计链路。

### 2. 工具清单

| 工具名称 | 工具能力说明 | 需要哪些输入 | 返回什么 |
|---|---|---|---|
| `build_recruitment_plan` | 生成患者招募计划和入组节奏 | Protocol、样本量、中心数量、招募周期 | Recruitment Plan |
| `track_recruitment_funnel` | 追踪筛查、入组、失访、完成情况 | 招募台账、随访状态 | 招募漏斗、风险提醒 |
| `generate_followup_schedule` | 生成随访计划和访视窗口 | Protocol、访视表、入组日期 | Follow-up Schedule |
| `audit_followup_deviation` | 识别超窗、失访、方案偏离 | 随访记录、访视窗口、方案规则 | Deviation Log |
| `build_site_qc_plan` | 生成站点/研究者质控计划 | 多中心信息、CRF 字段、质控标准 | Site QC Plan |
| `generate_monitoring_report` | 输出研究执行监查报告 | 招募、随访、CRF、偏离记录 | Monitoring Report |

### 3. 执行流程

步骤 1：读取已确认 Protocol、伦理要求、招募周期、中心信息和 CRF。  
步骤 2：生成招募计划、随访窗口和站点质控计划。  
步骤 3：强制中断——确认执行计划、随访窗口和质控规则。  
步骤 4：持续追踪招募漏斗、失访、超窗、CRF 缺失和方案偏离。  
步骤 5：生成执行质量摘要和 Monitoring Report。  
步骤 6：将可分析数据和执行偏离记录交接给 F-04 数据清洗。

**强制中断步骤**：步骤 3、步骤 5；执行策略调整、方案偏离记录、正式监查报告属于 R2。  
**边界**：患者联系、外部随访通知、方案偏离上报等动作需要授权；Agent 默认只生成计划、清单和报告草案。

### 4. 输出规范

1. **最终交付内容**：Recruitment Plan、Follow-up Schedule、Site QC Plan、Deviation Log、Monitoring Report。
2. **语气风格**：执行管理视角、风险前置、指标清晰。
3. **用户操作要求**：招募策略、随访窗口、方案偏离和正式报告必须确认。

### 5. 与其他任务衔接

1. **下一步**：F-04 数据清洗、F-06 统计分析；如发现合规偏离，回到 F-11。
2. **触发方式**：伦理/注册路径明确后，或用户上传执行台账、随访记录、CRF 数据时触发。
3. **上游任务**：F-03 研究设计、F-11 伦理注册。

---

## 附录 A：Agent 与工作流共享能力映射

| 功能 | Agent 负责 | 共享确定性能力/领域服务 |
|---|---|---|
| F-01 | 澄清意图、比较候选、解释评分 | 热点检索、PICOS Schema、数据集目录 |
| F-02 | 组织证据、识别争议、生成叙述 | 文献导入、元数据、证据卡片、引用校验 |
| F-03 | 设计候选与方法解释 | PICOS 校验、样本量计算器、方案 Schema |
| F-04 | 理解字段与提出策略 | 质量检测、清洗执行器、数据版本服务 |
| F-05 | 解释推荐与引导确认 | 数据画像、算法注册表、硬约束路由 |
| F-06 | 编排、诊断建议、解释结果 | Python/R Worker、结果校验器、溯源服务 |
| F-07 | 理解表达目标、调样式 | 绘图代码、渲染 Worker、Figure Asset |
| F-08 | 章节规划、写作和润色 | Verified Result、表格、引用与合规闸门 |
| F-09 | 处理歧义、引导人工确认 | 元数据解析、样式规则、完整性校验 |
| F-10 | 理解投稿偏好、解释匹配 | 期刊数据库、规则版本和风险评分 |
| F-11 | 伦理路径判断、材料组织、合规解释 | 伦理模板、注册字段、隐私治理规则 |
| F-12 | 执行计划组织、偏离提醒、质控解释 | 招募漏斗、随访窗口、CRF 质控、监查报告 |

## 附录 B：强制确认点汇总

| 功能 | 强制确认内容 | 风险等级 |
|---|---|---|
| F-01 | 研究方向、PICOS 草案 | R1 |
| F-02 | 文献纳入范围、综述结构 | R1 |
| F-03 | 研究设计、样本量参数、正式方案 | R2 |
| F-11 | 伦理路径、注册字段、知情同意、正式材料导出 | R2/R3 |
| F-12 | 招募计划、随访窗口、方案偏离、监查报告 | R2 |
| F-04 | 清洗规则和影响预览 | R2 |
| F-05 | 方法选择、变量映射、参数 | R2 |
| F-06 | 实际执行参数、模型调整 | R2 |
| F-07 | 图形类型、视觉编码、最终导出 | R2/R3 |
| F-08 | 章节定稿、正式文稿导出 | R3 |
| F-09 | 元数据冲突和推断字段 | R1/R3 |
| F-10 | 目标期刊与投稿梯队 | R3 |

---

> 本文定义的是 Agent 的任务编排与交互职责。算法公式、执行器实现、数据 Schema 和 API 契约应在对应技术设计中单独维护，避免 Agent 描述成为不可验证的业务逻辑来源。
